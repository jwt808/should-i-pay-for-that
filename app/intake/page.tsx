import { IntakeWizard } from "@/components/IntakeWizard";

export const metadata = { title: "Investigate My Repair — Should I Pay That?" };

export default function IntakePage() {
  return <IntakeWizard />;
}
